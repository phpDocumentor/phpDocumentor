{% spaceless %}
<figure>
    {{ renderNode(node.image) }}

    {% if node.document %}
        {% set caption = renderNode(node.document) %}

        {% if caption %}
            <figcaption>{{ caption|raw }}</figcaption>
        {% endif %}
    {% endif %}
</figure>
{% endspaceless %}
